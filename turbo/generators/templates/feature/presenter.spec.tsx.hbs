import { describe, expect, it } from "vitest";

import { use{{pascalCase name}}Presenter } from "./{{kebabCase name}}.presenter";

describe("use{{pascalCase name}}Presenter", () => {
  it("provides default metadata", () => {
    const presenter = use{{pascalCase name}}Presenter();

  expect(presenter.title).toBe("{{pascalCase name}}");
    expect(presenter.description).toContain("feature placeholder");
  });
});
