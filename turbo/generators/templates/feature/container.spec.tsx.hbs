import { render } from "vitest-browser-react";
import { describe, expect, it } from "vitest";

import { {{pascalCase name}}Container } from "./{{kebabCase name}}.container";

describe("{{pascalCase name}}Container", () => {
  it("renders the component with default content", async () => {
    await render(<{{pascalCase name}}Container />);

    const heading = document.querySelector(
      '[data-slot="{{kebabCase name}}-component"] h2'
    );
    const description = document.querySelector(
      '[data-slot="{{kebabCase name}}-component"] p'
    );

    expect(heading?.textContent ?? "").toContain("{{pascalCase name}}");
    expect(description?.textContent ?? "").toContain("feature placeholder");
  });
});
