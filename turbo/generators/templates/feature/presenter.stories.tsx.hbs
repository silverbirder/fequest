import type { Meta, StoryObj } from "@storybook/nextjs";

import { use{{pascalCase name}}Presenter } from "./{{kebabCase name}}.presenter";

const PresenterPreview = () => {
  const presenter = use{{pascalCase name}}Presenter();
  return (
    <pre className="rounded-md bg-muted p-4 text-sm">
      {JSON.stringify(presenter, null, 2)}
    </pre>
  );
};

const meta = {
  title: "Features/{{pascalCase app}}/{{pascalCase name}}/Presenter",
  component: PresenterPreview,
} satisfies Meta<typeof PresenterPreview>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {};
