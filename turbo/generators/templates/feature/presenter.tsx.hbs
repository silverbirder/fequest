type {{pascalCase name}}Presenter = {
  title: string;
  description: string;
};

const use{{pascalCase name}}Presenter = (): {{pascalCase name}}Presenter => {
  return {
    title: "{{pascalCase name}}",
    description: "{{pascalCase name}} feature placeholder",
  };
};

export type { {{pascalCase name}}Presenter };
export { use{{pascalCase name}}Presenter };
