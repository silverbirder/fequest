import { render } from "vitest-browser-react";
import { describe, expect, it } from "vitest";

import { {{pascalCase name}}Container } from "./container";

describe("{{pascalCase name}} feature", () => {
  it("renders presenter data", async () => {
    await render(<{{pascalCase name}}Container />);

    const element = document.querySelector('[data-slot="{{kebabCase name}}-feature"]');
    expect(element).not.toBeNull();
    expect(element?.textContent ?? "").toContain("{{pascalCase name}} feature");
  });
});
